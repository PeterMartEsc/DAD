{
  "name": "trabajo-api-lol",
  "version": "1.0.0",
  "description": "",

  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel serve -d dist src/index.html",
    "build": "parcel build -d build --public-url /DAD/ src/index.html",
    "deploy": "gh-pages -d build"
},
  "author": "Pedro Martin Escuela",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.12.0"
  }
}
